#!/usr/bin/env zsh
#
# Claude Code provider key pools (example / redacted).
#
# Copy to your machine as: ~/.claude-keys.zsh
# Then replace the placeholder values with your real keys.
#
# Never commit real keys to git or share them in screenshots.
#

# NVIDIA NIM keys (nvapi-...)
_NVIDIA_KEYS=(
  "nvapi-REPLACE_ME_1"
  "nvapi-REPLACE_ME_2"
)

# OpenRouter keys (sk-or-...)
_OPENROUTER_KEYS=(
  "sk-or-REPLACE_ME_1"
  "sk-or-REPLACE_ME_2"
)

# Optional: Groq keys (gsk-...) if you still use Groq
_GROQ_KEYS=()

